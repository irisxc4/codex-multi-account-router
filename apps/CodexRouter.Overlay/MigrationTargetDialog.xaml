<Window x:Class="CodexRouter.Overlay.MigrationTargetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CodexRouter.Overlay"
        Title="{x:Static local:UiText.MigrationDialogTitle}" Width="360" Height="330"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" Background="#17191E">
    <Border BorderBrush="#464B55" BorderThickness="1" CornerRadius="12" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="{x:Static local:UiText.MigrationDialogTitle}" FontSize="15" FontWeight="SemiBold" />
            <TextBlock Grid.Row="1"
                       Text="{x:Static local:UiText.MigrationDialogDescription}"
                       Foreground="#9297A1" FontSize="10.5" TextWrapping="Wrap" Margin="0,5,0,12" />
            <ListBox x:Name="AccountList" Grid.Row="2"
                     Background="Transparent" BorderThickness="0"
                     Foreground="#F4F4F5" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#343841" BorderThickness="1" CornerRadius="8"
                                Padding="10,8" Margin="0,0,0,7" Background="#22262D">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Alias}" FontWeight="SemiBold" FontSize="12" />
                                    <TextBlock Text="{Binding Detail}" Foreground="#8D929C" FontSize="9.5" Margin="0,2,0,0" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding ShortQuota}" FontSize="9.5" Foreground="#B6BAC2" HorizontalAlignment="Right" />
                                    <TextBlock Text="{Binding Health}" FontSize="9" Foreground="#8D929C" HorizontalAlignment="Right" Margin="0,2,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Style="{StaticResource FlatButton}" Content="{x:Static local:UiText.Cancel}" Click="OnCancel" Margin="0,0,8,0" />
                <Button Style="{StaticResource FlatButton}" Content="{x:Static local:UiText.CreateMigratedThread}" Click="OnContinue" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
